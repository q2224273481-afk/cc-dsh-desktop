// Host half of dsh-desktop-polish: intentionally empty.
// Pure browser-side capability — the row exists so the Loader entry is present
// in the host tree, while the polish logic ships through exports["./client"].
export function apply() {}
