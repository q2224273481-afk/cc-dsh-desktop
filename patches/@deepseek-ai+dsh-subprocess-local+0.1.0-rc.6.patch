diff --git a/node_modules/@deepseek-ai/dsh-subprocess-local/lib/index.js b/node_modules/@deepseek-ai/dsh-subprocess-local/lib/index.js
index 75fae74..937861f 100644
--- a/node_modules/@deepseek-ai/dsh-subprocess-local/lib/index.js
+++ b/node_modules/@deepseek-ai/dsh-subprocess-local/lib/index.js
@@ -497,7 +497,7 @@ function taskkillProcessTree(pid) {
 		String(pid),
 		"/T",
 		"/F"
-	], { stdio: "ignore" });
+	], { stdio: "ignore", windowsHide: true });
 }
 /**
 * Signal a detached process tree with platform-correct semantics: POSIX
@@ -553,7 +553,11 @@ function spawnSubprocess(spec, internals = {}) {
 			outMode === "inherit" ? "inherit" : "pipe",
 			errMode === "inherit" ? "inherit" : "pipe"
 		],
-		detached: platform !== "win32"
+		detached: platform !== "win32",
+		// Desktop-shell patch: GUI hosts have no console to inherit, so without
+		// CREATE_NO_WINDOW every console-subsystem child (pwsh/bash/cmd/…)
+		// flashes a console window on Windows. Ignored on other platforms.
+		windowsHide: true
 	});
 	const collectStream = (mode, stream, label) => {
 		if (!isCollect(mode) || stream === null) return void 0;
